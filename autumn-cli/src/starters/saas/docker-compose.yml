services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: {{crate_name}}
      POSTGRES_USER: autumn
      POSTGRES_PASSWORD: autumn
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
