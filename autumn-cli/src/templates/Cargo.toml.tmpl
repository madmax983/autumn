[package]
name = "{{project_name}}"
version = "0.1.0"
edition = "2024"

[dependencies]
# Enable optional subsystems later by switching to:
# autumn-web = { version = "{{autumn_version}}", features = ["redis", "mail"] }
autumn-web = "{{autumn_version}}"

[workspace]
