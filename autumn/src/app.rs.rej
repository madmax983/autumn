--- autumn/src/app.rs
+++ autumn/src/app.rs
@@ -308,6 +308,9 @@
     /// Can be called multiple times -- routes are combined additively.
     /// Use the [`routes!`](crate::routes) macro to collect annotated
     /// handlers into the expected `Vec<Route>`.
+    ///
+    /// *Note: if you encounter a compiler error mentioning `__autumn_route_info_...`,
+    /// it means a route handler passed to `routes!` was misspelled or not found.*
     ///
     /// # Examples
     ///
