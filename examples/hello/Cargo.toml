[package]
name = "hello"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
autumn-web = { path = "../../autumn" }
autumn-monitor-plugin = { path = "../../autumn-monitor-plugin" }

[lints.rust]
unsafe_code = "forbid"
