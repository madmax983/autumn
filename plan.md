1. **Fix the CI Script**: The error states `No such file or directory` for `/home/runner/work/autumn/autumn/autumn-harvest`. Looking at the repository structure `ls -la`, we can see the `autumn-harvest` directory actually *does* exist. The CI failure is not due to a missing directory, but perhaps due to an action checkout misconfiguration or caching problem. However, looking at the logs, it appears `actions/checkout@v4` successfully pulled the branch. Let's look closer: the repository root is indeed `/home/runner/work/autumn/autumn`. The file `.github/workflows/ci.yml` is defining `working-directory: autumn-harvest`. The repository currently has an `autumn-harvest` directory containing `autumn-harvest`, `autumn-harvest-plugin`, and `autumn-harvest-macros`. Let's remove the `working-directory: autumn-harvest` configuration from all steps in `.github/workflows/ci.yml` as `cargo` commands with `-p` flag work globally from the workspace root.
2. Use `run_in_bash_session` to remove `working-directory: autumn-harvest` from `.github/workflows/ci.yml`.
3. Use `run_in_bash_session` to run `cargo test --workspace` to ensure no regressions were introduced locally.
4. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
5. Use the `submit` tool with the same branch name `havoc-chaos-channels` and a new commit message to push the code changes.
